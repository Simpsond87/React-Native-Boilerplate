// @flow

import React, { Component } from 'react';
import { ScrollView, Image, View, Text } from 'react-native';

// Styles
import styles from './Styles/{{properCase name}}';

export default class {{ properCase name }} extends React.Component {
  render() {
    return (
      <ScrollView>

      </ScrollView>
    );
  }
}
